# Copy this file to `.env` and adjust values as needed.
# These variables are loaded by `python-dotenv` in local dev.

# Spotify API credentials (create an app at https://developer.spotify.com)
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# Database URL (SQLite by default). Do not commit the DB file.
# Examples:
#   SQLite (local file): sqlite:///whencejam.db
#   SQLite (memory for tests): sqlite+pysqlite:///:memory:
#   Postgres: postgresql+psycopg://user:pass@localhost:5432/whencejam
WHENCEJAM_DB_URL=sqlite:///whencejam.db

